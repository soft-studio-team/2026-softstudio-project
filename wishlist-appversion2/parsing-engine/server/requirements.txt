# 파싱 엔진이 사용하는 파이썬 패키지 목록
# 설치: pip install -r requirements.txt
#
# 주의: 이전 버전에 있던 playwright(헤드리스 브라우저)는 의도적으로 제거했습니다.
# 웹뷰/브라우저 렌더링 방식은 약관·안정성·성능 문제로 기각(전략 문서 2절)했고,
# 새 엔진은 HTTP GET 1회 + 표준 메타데이터 파싱만 사용합니다.

beautifulsoup4==4.12.3   # HTML 파서 (JSON-LD <script>, OG <meta> 추출용)
lxml==5.2.2              # BeautifulSoup 백엔드 (빠른 HTML 분석기)
fastapi==0.111.0         # API 서버 프레임워크 (/parse 제공)
uvicorn==0.30.1          # FastAPI 실행기

# 개발·테스트용
pytest==8.2.2
